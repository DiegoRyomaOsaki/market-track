# pnpm >= 11 lee de este archivo SOLO ajustes de auth y de registry. Los ajustes
# de comportamiento (nodeLinker, hoistPattern, shamefullyHoist,
# onlyBuiltDependencies...) van en pnpm-workspace.yaml — puestos aquí se ignoran
# en silencio, sin error.
registry=https://registry.npmjs.org/
