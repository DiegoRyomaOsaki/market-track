import type { Metadata } from "next";

import { SeccionPlaceholder } from "@/components/panel/seccion-placeholder";

export const metadata: Metadata = { title: "Clientes-marca — Market Track" };

export default function ClientesMarcaPage() {
  return <SeccionPlaceholder titulo="Clientes-marca" />;
}
