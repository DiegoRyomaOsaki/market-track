import type { Metadata } from "next";

import { SeccionPlaceholder } from "@/components/panel/seccion-placeholder";

export const metadata: Metadata = { title: "Ruteros — Market Track" };

export default function RuterosPage() {
  return <SeccionPlaceholder titulo="Ruteros" />;
}
