// Tailwind v4 es CSS-first: no hay `tailwind.config.js`; el plugin de PostCSS es
// todo lo que hace falta. La configuración del tema vive en `globals.css`.
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
