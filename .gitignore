# Dependencies
node_modules/

# Env & secrets
.env
.env.*
!.env.example
secrets/

# Build outputs
.next/
.expo/
.turbo/
dist/
build/
out/

# Supabase local
supabase/.temp/
supabase/.branches/

# Claude Code local settings
.claude/settings.local.json

# claude-kit source (bootstrap kit — already adapted into .claude/; kept local
# for /sync-check, not part of the project)
claude-kit/

# OS / editor
.DS_Store
Thumbs.db
*.log

# Incremental de tsc (apps/web).
*.tsbuildinfo

# Generado por Next.js (regenerado en cada build). El typecheck no lo necesita:
# apps/web/src/styles.d.ts declara los imports de CSS.
next-env.d.ts

# Mapa base de desarrollo: lo baja `pnpm --filter @market-track/web tiles:dev`
# (9,4 MiB). No va a git: en producción vive en R2 (ADR-0009).
apps/web/public/tiles/
